diff -ruN portfolio_patch_old/app.py portfolio_patch/app.py
--- portfolio_patch_old/app.py	2026-06-24 07:20:18.000000000 +0000
+++ portfolio_patch/app.py	2026-06-24 07:29:22.653795463 +0000
@@ -5213,6 +5213,9 @@
     "系统策略得分": "按年化超额收益、最大回撤控制、夏普比率、卡玛比率、执行质量综合评估系统策略表现。",
     "定投策略得分": "按年化超额收益、最大回撤控制、夏普比率、卡玛比率、执行质量综合评估定投基准表现。",
     "持有策略得分": "按年化超额收益、最大回撤控制、夏普比率、卡玛比率、执行质量综合评估一次性买入持有基准表现。",
+    "组合策略得分": "按组合策略权益的年化超额收益、最大回撤控制、夏普比率、卡玛比率和执行质量综合评估组合策略表现。",
+    "组合定投得分": "按组合定投基准权益的年化超额收益、最大回撤控制、夏普比率、卡玛比率和执行质量综合评估组合定投表现。",
+    "组合持有得分": "按组合买入持有基准权益的年化超额收益、最大回撤控制、夏普比率、卡玛比率和执行质量综合评估组合持有表现。",
     "卡玛比率": "（策略年化收益 - 无风险收益率）/ 策略最大回撤绝对值，越高表示单位回撤换来的超额收益越高。",
     "定投策略卡玛比率": "（定投策略年化收益 - 无风险收益率）/ 定投策略最大回撤绝对值，用作定投基准的风险收益比。",
     "持有策略卡玛比率": "（买入持有年化收益 - 无风险收益率）/ 买入持有最大回撤绝对值，用作买入持有基准的风险收益比。",
@@ -6754,6 +6757,7 @@
     # 前端会按同一顺序渲染；导出的核心指标 CSV 也保持这个顺序。
     order = [
         "标的核心得分", "系统策略得分", "定投策略得分", "持有策略得分",
+        "组合策略得分", "组合定投得分", "组合持有得分",
         "策略年化收益", "定投策略年化收益", "买入持有年化收益",
         "策略最大回撤", "定投策略最大回撤", "买入持有最大回撤",
         "夏普比率", "定投策略夏普比率", "持有策略夏普比率",
@@ -6906,6 +6910,10 @@
             "asset_pct": str(row.get("当前总资产") or ""),
             "action": str(row.get("操作建议") or ""),
             "rule": str(row.get("命中规则") or ""),
+            "symbol": str(row.get("标的") or ""),
+            "symbol_name": str(row.get("名称") or ""),
+            "plan_weight_pct": str(row.get("计划占比%") or ""),
+            "amount": str(row.get("成交金额") or ""),
         }
         out.append(point)
     return out
@@ -7300,6 +7308,8 @@
             "end": backtest_chart_series[-1].get("date") if backtest_chart_series else "",
             "symbol": symbol,
             "market": market,
+            "chart_type": "single_asset",
+            "line_labels": {"close": "基金净值/收盘", "ma20": "MA20", "ma50": "MA50", "ma200": "MA200"},
         },
         "exported": exported,
 
@@ -7402,6 +7412,9 @@
         })
         chart_series.append({
             "date": d,
+            # 组合趋势图使用权益指数口径：初始计划资金=100。
+            # 为了兼容前端原有画线字段，close/ma20/ma50 分别承载
+            # 组合策略权益、组合定投权益、组合买入持有权益，而不是单只基金净值/均线。
             "close": round(strategy_total / max(total_cash, 1e-9) * 100.0, 6),
             "ma20": round(dca_total / max(total_cash, 1e-9) * 100.0, 6),
             "ma50": round(hold_total / max(total_cash, 1e-9) * 100.0, 6),
@@ -7580,6 +7593,9 @@
             "end": chart_series[-1].get("date") if chart_series else "",
             "symbol": "PORTFOLIO",
             "market": "组合",
+            "chart_type": "portfolio",
+            "line_labels": {"close": "组合策略权益指数", "ma20": "组合定投权益指数", "ma50": "组合持有权益指数"},
+            "chart_note": "组合图展示的是组合权益指数，初始计划资金=100；买卖点来自各基金独立策略交易，圆点贴在组合策略权益线。",
         },
         "exported": exported,
         "fetch_errors": fetch_errors,
diff -ruN portfolio_patch_old/templates/index.html portfolio_patch/templates/index.html
--- portfolio_patch_old/templates/index.html	2026-06-24 07:20:18.000000000 +0000
+++ portfolio_patch/templates/index.html	2026-06-24 07:30:31.844940616 +0000
@@ -464,7 +464,7 @@
     </header>
     <div class="trend-chart-tools">
       <span id="trend-chart-meta" class="small">拉取数据后可查看最近约一年趋势。</span>
-      <div class="trend-chart-legend" aria-label="图例">
+      <div id="trend-chart-legend" class="trend-chart-legend" aria-label="图例">
         <span><i data-line="close"></i>净值/收盘</span>
         <span><i data-line="ma20"></i>MA20</span>
         <span><i data-line="ma50"></i>MA50</span>
@@ -1137,6 +1137,24 @@
     {key: "ma50", label: "MA50", color: "#2d63b8", width: 1.8},
     {key: "ma200", label: "MA200", color: "#b65d2a", width: 1.8}
   ];
+  function isPortfolioBacktestChart() {
+    return lastChartMode === "backtest" && String(lastChartMeta?.chart_type || "").toLowerCase() === "portfolio";
+  }
+  function chartLinesForCurrentMode() {
+    const labels = (lastChartMeta && typeof lastChartMeta.line_labels === "object") ? lastChartMeta.line_labels : {};
+    const base = CHART_LINES.map(line => ({...line, label: labels[line.key] || line.label}));
+    if (isPortfolioBacktestChart()) return base.filter(line => line.key !== "ma200");
+    return base;
+  }
+  function renderTrendChartLegend(lines = chartLinesForCurrentMode()) {
+    const host = $("#trend-chart-legend");
+    if (!host) return;
+    const lineHtml = lines.map(line => `<span><i data-line="${escapeHtml(line.key)}"></i>${escapeHtml(line.label)}</span>`).join("");
+    const tradeHtml = lastChartMode === "backtest"
+      ? `<span><i data-line="buy"></i>买入点</span><span><i data-line="sell"></i>卖出点</span>`
+      : "";
+    host.innerHTML = `${lineHtml}${tradeHtml}`;
+  }
 
   function finiteNumber(value) {
     if (value === null || value === undefined || value === "") return null;
@@ -1169,6 +1187,10 @@
       asset_pct: String(row?.asset_pct || row?.["当前总资产"] || ""),
       action: String(row?.action || row?.操作建议 || ""),
       rule: String(row?.rule || row?.命中规则 || ""),
+      symbol: String(row?.symbol || row?.标的 || ""),
+      symbol_name: String(row?.symbol_name || row?.名称 || ""),
+      plan_weight_pct: String(row?.plan_weight_pct || row?.["计划占比%"] || ""),
+      amount: String(row?.amount || row?.成交金额 || ""),
     })).filter(row => row.date && row.direction);
   }
   function updateTrendChartButton() {
@@ -1224,7 +1246,15 @@
     const last = lastChartSeries[lastChartSeries.length - 1] || {};
     const prev = lastChartSeries.length >= 2 ? lastChartSeries[lastChartSeries.length - 2] : null;
     const dayRet = prev && prev.close ? (last.close / prev.close - 1) * 100 : null;
-    const rows = [
+    const portfolioMode = isPortfolioBacktestChart();
+    const rows = portfolioMode ? [
+      ["组合策略权益指数", fmtChartValue(last.close, "close")],
+      ["策略日收益", fmtChartValue(dayRet, "return")],
+      ["组合定投权益指数", fmtChartValue(last.ma20, "ma20")],
+      ["组合持有权益指数", fmtChartValue(last.ma50, "ma50")],
+      ["交易点", `${lastChartTrades.length}个`],
+      ["样本", `${lastChartSeries.length}条`],
+    ] : [
       ["最新", fmtChartValue(last.close, "close")],
       ["日涨跌", fmtChartValue(dayRet, "return")],
       ["MA20", fmtChartValue(last.ma20, "ma20")],
@@ -1252,8 +1282,12 @@
     const card = $("#trend-chart-hover-card");
     if (!card || !marker) return;
     const reason = String(marker.trade?.rule || marker.trade?.action || "暂无操作原因").trim() || "暂无操作原因";
+    const asset = [marker.trade?.symbol_name, marker.trade?.symbol].filter(Boolean).join(" · ");
+    const plan = marker.trade?.plan_weight_pct ? `计划占比 ${marker.trade.plan_weight_pct}%` : "";
+    const action = `${marker.trade?.direction || ""}${marker.trade?.trade_pct !== null && marker.trade?.trade_pct !== undefined ? ` ${marker.trade.trade_pct}%` : ""}`.trim();
     card.innerHTML = `
-      <strong>操作原因</strong>
+      <strong>${escapeHtml(asset || "操作原因")}</strong>
+      <div class="muted">${escapeHtml([action, plan, marker.trade?.amount ? `成交金额 ${marker.trade.amount}` : ""].filter(Boolean).join(" · "))}</div>
       <div class="reason-text">${escapeHtml(reason)}</div>
     `;
     card.hidden = false;
@@ -1371,11 +1405,22 @@
     const meta = $("#trend-chart-meta");
     renderTrendChartKpis();
     const title = $("#trend-chart-title");
-    if (title) title.textContent = lastChartMode === "backtest" ? "回测趋势 / 买卖点" : "净值 / 均线趋势";
+    const portfolioMode = isPortfolioBacktestChart();
+    const chartLines = chartLinesForCurrentMode();
+    renderTrendChartLegend(chartLines);
+    if (title) {
+      title.textContent = portfolioMode
+        ? "组合回测趋势 / 各基金买卖点"
+        : (lastChartMode === "backtest" ? "单标的回测趋势 / 买卖点" : "净值 / 均线趋势");
+    }
     if (meta) {
       const start = lastChartSeries[0]?.date || lastChartMeta.start || "--";
       const end = lastChartSeries[lastChartSeries.length - 1]?.date || lastChartMeta.end || "--";
-      if (lastChartMode === "backtest") {
+      if (portfolioMode) {
+        meta.textContent = lastChartSeries.length
+          ? `${start} 至 ${end} · ${lastChartSeries.length}条 · 组合权益指数初始=100 · 买卖点${lastChartTrades.length}个（各基金交易）`
+          : "暂无组合回测趋势图数据，请先运行历史回测。";
+      } else if (lastChartMode === "backtest") {
         meta.textContent = lastChartSeries.length ? `${start} 至 ${end} · ${lastChartSeries.length}条 · 买卖点${lastChartTrades.length}个` : "暂无回测趋势图数据，请先运行历史回测。";
       } else {
         meta.textContent = lastChartSeries.length ? `${start} 至 ${end} · ${lastChartSeries.length}条 · 拉取后本地绘制` : "暂无数据，请先拉取标的数据。";
@@ -1407,7 +1452,7 @@
     const plotH = Math.max(10, h - pad.top - pad.bottom);
     const allValues = [];
     for (const row of lastChartSeries) {
-      for (const line of CHART_LINES) {
+      for (const line of chartLines) {
         const v = finiteNumber(row[line.key]);
         if (v !== null) allValues.push(v);
       }
@@ -1460,7 +1505,7 @@
     ctx.strokeStyle = "rgba(47, 36, 29, .28)";
     ctx.strokeRect(pad.left, pad.top, plotW, plotH);
 
-    for (const line of CHART_LINES) {
+    for (const line of chartLines) {
       ctx.beginPath();
       let started = false;
       let points = 0;
