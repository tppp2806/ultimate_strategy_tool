# v22：结果大字显示本次仓位变化

## 修复

- 买入/加仓时，结果卡片大字改为显示“本次需要增加的仓位”。
  - 例如当前仓位 50%，目标仓位 60%，显示 `+10%`。
  - 目标总仓位仍显示在副标题和指标里。
- 减仓/止盈时同理显示“本次需要减少的仓位”。
  - 例如当前仓位 60%，目标仓位 40%，显示 `-20%`。

## 覆盖文件

```text
app.py
```
