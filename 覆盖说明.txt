覆盖路径：
- app.py -> 项目根目录/app.py
- templates/index.html -> 项目根目录/templates/index.html
- static/app.js -> 项目根目录/static/app.js
- static/style.css -> 项目根目录/static/style.css

修复内容：
1. 修复点击顶部【参数】按钮导致策略信号被清空的问题：
   - 内联脚本不再把【参数】按钮点击误判为总体策略切换。
   - 动态策略信号区重绘前会保留已选中的单选/多选/文本数值。
   - 参数弹窗打开事件增加 stopImmediatePropagation 隔离，避免被后续全局监听误处理。

2. 统一所有滚动条/滑动条样式：
   - 覆盖左右/中间面板、设置弹窗、参数弹窗、搜索候选、回测表格、自动数据弹窗等滚动条。
   - 同时补充 input[type="range"] 的统一轨道和滑块样式，方便后续如果使用 range 控件也保持一致。

已做检查：
- static/app.js 已通过 node --check。
- templates/index.html 内的两段普通内联脚本已分别通过 node --check。
