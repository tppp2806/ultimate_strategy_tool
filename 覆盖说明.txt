覆盖说明：
1. 将 app.py 覆盖到项目根目录的 app.py。
2. 将 templates/index.html 覆盖到项目根目录的 templates/index.html。

修复内容：
- 历史回测交易记录和趋势图买卖点的“操作原因”改为按真实成交方向展示。
- 当真实成交是卖出时，不再显示“策略买入 / 均线回调买入：策略买入”。
- 对简易均线 + 定投增强模式做了兼容：如果策略信号是买入，但目标仓位低于当前仓位，红色卖点会显示为“目标仓位低于当前仓位，执行卖出至 xx%”。
- 前端趋势图也加了兜底：旧回测数据里若仍带有买入字样，悬停红色卖点时会自动按卖出口径展示。

建议：覆盖后重新运行一次历史回测，旧页面缓存的回测结果不会自动重算。
