# v24 结果卡片显示修复

## 改动

1. 移除结果卡片顶部动作行
   - 不再显示顶部的 `清仓-10%`、`持有` 等重复文字。

2. 大号结果显示完整动作
   - 加仓：显示 `加仓+12%`
   - 买入：显示 `买入+12%`
   - 减仓：显示 `减仓-30%`
   - 止盈：显示 `止盈-20%`
   - 清仓：显示 `清仓-10%`
   - 持有：显示 `维持 10%`

3. 保留副标题解释
   - 大号结果下方仍显示规则解释。

## 覆盖文件

```text
app.py
templates/index.html
static/app.js
```
