自动数据弹窗只读表格补丁

覆盖路径：
- app.py
- templates/index.html
- strategies/factors/mini_factors.py

本次修复：
1. 【查看全部自动数据】弹窗改为只读预览，不再生成可编辑 input。
2. 弹窗改为传统紧凑表格：字段 / 值 / Key / 说明。
3. 保留过滤搜索与分组展示。
4. 自动拉取到的全部指标仍会写入 signal-form 的隐藏字段，继续参与策略计算。
5. 外层仍只保留关键手动覆盖项，避免和只读预览重复。

使用：
解压到项目根目录覆盖后，重启 Flask，并 Ctrl+F5 强制刷新浏览器。
