# v9：修复【大盘/板块同步走弱】单独占一行

## 改动

1. 将【大盘/板块同步走弱】从独立 `check-grid` 移入【大趋势环境】同一个网格中。
2. 新增 `market-grid`，让该区域统一按卡片网格排列，不再出现一个选项单独铺满整行。
3. 新增 `mixed-choice`，避免辅助复选项被当作单选组参与同组降权：
   - 大趋势状态仍然互斥、未选项会淡化；
   - 【大盘/板块同步走弱】是辅助风险项，勾选后不会把趋势选项错误淡化。

## 覆盖文件

```text
templates/index.html
static/style.css
```
